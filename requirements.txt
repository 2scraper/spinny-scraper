# Core — install at least one browser engine:
playwright>=1.40.0
# selenium>=4.15.0
# pyppeteer>=2.0.0

# For direct API mode (recommended — no browser needed):
httpx>=0.27.0
# or: requests>=2.31.0  (for Selenium sync version)

# Optional: Selenium auto-driver management
webdriver-manager>=4.0.0
# Optional: Selenium network interception
# selenium-wire>=5.1.0

# CAPTCHA solving via 2captcha.com
2captcha-python>=1.3.0
